import { LazyLoading } from './lazy-loading';

describe('LazyLoading', () => {
  it('should create an instance', () => {
    const directive = new LazyLoading();
    expect(directive).toBeTruthy();
  });
});
